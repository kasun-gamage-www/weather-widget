import { DistanceUnitPipe } from './distance-unit.pipe';

describe('DistanceUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
